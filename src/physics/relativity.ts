/**
 * Redundancia detectada:
 * este modulo queda como capa de compatibilidad para no romper imports viejos.
 * La fuente real de verdad vive en `schwarzschildPhysics.ts`.
 */
export {
  computeRedshift,
  computeSchwarzschildRadius,
  computeTimeDilation,
} from "./schwarzschildPhysics"
